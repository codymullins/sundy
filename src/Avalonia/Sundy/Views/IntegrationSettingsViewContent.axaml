<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Sundy.ViewModels"
             x:Class="Sundy.Views.IntegrationSettingsViewContent"
             x:DataType="vm:CalendarSettingsViewModel">
    <StackPanel Spacing="20">
        <!-- Header -->
        <TextBlock Text="Integrations"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource ForegroundPrimary}" />

        <TextBlock Text="Connect your external calendars to sync events across all your services."
                   Foreground="{DynamicResource ForegroundSecondary}"
                   TextWrapping="Wrap"
                   FontSize="14" />

        <!-- Microsoft Outlook Integration -->
        <Border BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20"
                Background="{DynamicResource BackgroundSecondary}">
            <Grid RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="Auto,*,Auto">
                <Border Grid.Row="0" Grid.Column="0"
                        Width="48"
                        Height="48"
                        Background="#0078D4"
                        CornerRadius="8"
                        VerticalAlignment="Center"
                        Margin="0,0,16,0">
                    <TextBlock Text="O"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>

                <StackPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                    <TextBlock Text="Microsoft Outlook"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource ForegroundPrimary}" />
                    <!-- Show different text based on connection status -->
                    <TextBlock Text="Connect your Outlook calendar to sync events"
                               FontSize="13"
                               Foreground="{DynamicResource ForegroundSecondary}"
                               IsVisible="{Binding !IsOutlookConnected}" />
                    <StackPanel Orientation="Horizontal" Spacing="4" IsVisible="{Binding IsOutlookConnected}">
                        <TextBlock Text="Connected as"
                                   FontSize="13"
                                   Foreground="{DynamicResource ForegroundSecondary}" />
                        <TextBlock Text="{Binding OutlookUserName}"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource AccentPrimary}" />
                    </StackPanel>
                </StackPanel>

                <!-- Connect Button (shown when not connected) -->
                <Button Grid.Row="0" Grid.Column="2"
                        Content="Connect"
                        Command="{Binding ConnectOutlookCommand}"
                        Classes="accent"
                        MinWidth="100"
                        VerticalAlignment="Center"
                        IsVisible="{Binding !IsOutlookConnected}"
                        IsEnabled="{Binding !IsOutlookConnecting}" />

                <!-- Connecting indicator -->
                <StackPanel Grid.Row="0" Grid.Column="2"
                            Orientation="Horizontal"
                            Spacing="8"
                            VerticalAlignment="Center"
                            IsVisible="{Binding IsOutlookConnecting}">
                    <!-- Simple connecting text when NOT using device code -->
                    <TextBlock Text="Connecting..."
                               FontSize="13"
                               Foreground="{DynamicResource ForegroundSecondary}"
                               IsVisible="{Binding !IsDeviceCodeFlowActive}" />

                    <!-- Device code flow UI -->
                    <StackPanel Spacing="4" IsVisible="{Binding IsDeviceCodeFlowActive}">
                        <TextBlock Text="Sign in on another device"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource ForegroundPrimary}" />
                    </StackPanel>
                </StackPanel>

                <!-- Disconnect Button (shown when connected) -->
                <Button Grid.Row="0" Grid.Column="2"
                        Content="Disconnect"
                        Command="{Binding DisconnectOutlookCommand}"
                        Classes="danger"
                        MinWidth="100"
                        VerticalAlignment="Center"
                        IsVisible="{Binding IsOutlookConnected}" />

                <!-- Device Code Information Panel -->
                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                            Spacing="12"
                            Margin="0,12,0,0"
                            IsVisible="{Binding IsDeviceCodeFlowActive}">

                    <Border Background="{DynamicResource BackgroundTertiary}"
                            BorderBrush="{DynamicResource AccentPrimary}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="16">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Complete sign-in on another device"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource ForegroundPrimary}" />

                            <TextBlock Text="Open this URL in a web browser on any device:"
                                       FontSize="12"
                                       Foreground="{DynamicResource ForegroundSecondary}" />

                            <!-- URL with Copy Button -->
                            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                <Border Grid.Column="0"
                                        Background="{DynamicResource BackgroundPrimary}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="10,8">
                                    <TextBlock Text="{Binding DeviceCodeUrl}"
                                               FontFamily="Consolas,Menlo,Monaco,monospace"
                                               FontSize="13"
                                               Foreground="{DynamicResource AccentPrimary}"
                                               VerticalAlignment="Center" />
                                </Border>

                                <Button Grid.Column="1"
                                        Content="Copy"
                                        Command="{Binding CopyDeviceCodeUrlCommand}"
                                        Classes="accent"
                                        MinWidth="70"
                                        Height="36" />
                            </Grid>

                            <TextBlock Text="Then enter this code:"
                                       FontSize="12"
                                       Foreground="{DynamicResource ForegroundSecondary}"
                                       Margin="0,8,0,0" />

                            <!-- Code with Copy Button -->
                            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                <Border Grid.Column="0"
                                        Background="{DynamicResource BackgroundPrimary}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="10,8">
                                    <TextBlock Text="{Binding DeviceCodeUserCode}"
                                               FontFamily="Consolas,Menlo,Monaco,monospace"
                                               FontSize="20"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource AccentPrimary}"
                                               LetterSpacing="2"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center" />
                                </Border>

                                <Button Grid.Column="1"
                                        Content="Copy"
                                        Command="{Binding CopyDeviceCodeCommand}"
                                        Classes="accent"
                                        MinWidth="70"
                                        Height="36" />
                            </Grid>

                            <TextBlock Text="Waiting for sign-in..."
                                       FontSize="12"
                                       FontStyle="Italic"
                                       Foreground="{DynamicResource ForegroundDisabled}"
                                       HorizontalAlignment="Center"
                                       Margin="0,4,0,0" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Error message -->
                <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3"
                           Text="{Binding OutlookConnectionError}"
                           Foreground="{DynamicResource DangerPrimary}"
                           FontSize="12"
                           TextWrapping="Wrap"
                           Margin="0,8,0,0"
                           IsVisible="{Binding OutlookConnectionError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
            </Grid>
        </Border>

        <!-- Google Calendar Integration -->
        <Border BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20"
                Background="{DynamicResource BackgroundSecondary}">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <Border Grid.Column="0"
                        Width="48"
                        Height="48"
                        Background="#4285F4"
                        CornerRadius="8"
                        VerticalAlignment="Center"
                        Margin="0,0,16,0">
                    <TextBlock Text="G"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                    <TextBlock Text="Google Calendar"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource ForegroundPrimary}" />
                    <TextBlock Text="Connect your Google calendar to sync events"
                               FontSize="13"
                               Foreground="{DynamicResource ForegroundSecondary}" />
                </StackPanel>

                <Button Grid.Column="2"
                        Content="Connect"
                        Classes="accent"
                        MinWidth="100"
                        VerticalAlignment="Center" />
            </Grid>
        </Border>

        <!-- Apple Calendar Integration -->
        <Border BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20"
                Background="{DynamicResource BackgroundSecondary}">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <Border Grid.Column="0"
                        Width="48"
                        Height="48"
                        Background="#FF3B30"
                        CornerRadius="8"
                        VerticalAlignment="Center"
                        Margin="0,0,16,0">
                    <TextBlock Text="A"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                    <TextBlock Text="Apple Calendar"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource ForegroundPrimary}" />
                    <TextBlock Text="Connect your Apple calendar via CalDAV"
                               FontSize="13"
                               Foreground="{DynamicResource ForegroundSecondary}" />
                </StackPanel>

                <Button Grid.Column="2"
                        Content="Connect"
                        Classes="accent"
                        MinWidth="100"
                        VerticalAlignment="Center" />
            </Grid>
        </Border>

        <!-- CalDAV/iCal Integration -->
        <Border BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20"
                Background="{DynamicResource BackgroundSecondary}">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <Border Grid.Column="0"
                        Width="48"
                        Height="48"
                        Background="#6B7280"
                        CornerRadius="8"
                        VerticalAlignment="Center"
                        Margin="0,0,16,0">
                    <TextBlock Text="C"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                    <TextBlock Text="CalDAV / iCal"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource ForegroundPrimary}" />
                    <TextBlock Text="Connect any CalDAV or iCal compatible calendar"
                               FontSize="13"
                               Foreground="{DynamicResource ForegroundSecondary}" />
                </StackPanel>

                <Button Grid.Column="2"
                        Content="Add"
                        Classes="accent"
                        MinWidth="100"
                        VerticalAlignment="Center" />
            </Grid>
        </Border>
    </StackPanel>
</UserControl>
